{
  "name": "@organigrad/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx watch src/api/bootstrap.ts",
    "start": "node dist/api/bootstrap.js",
    "check": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "@apps2026/contracts": "github:Nouvelleeredigital2/apps2026-contracts#v1.1.0",
    "@fastify/cors": "^11.2.0",
    "fastify": "^5.0.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.0",
    "typescript": "~5.9.3",
    "vitest": "^4.0.18",
    "zod": "^3.25.76"
  }
}
